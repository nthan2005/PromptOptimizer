# yaml-language-server: $schema=../templates.schema.json
# source_id: 1597
# teaser: Create clean nextjs code without description
- id: 1597-clean-code
  title: Clean code
  tags:
    - configuration-management
    - clean
    - code
  family: Configuration Management
  body:
    Create a code for nextjs in full [TARGETLANGUAGE], where the function exists
    and its return() with its respective export default of [PROMPT]. I only want the
    code and I don't want text that describes that code or that explains it, optimize
    it as much as you can
  enabled: true
  required:
    - prompt
    - targetlanguage
