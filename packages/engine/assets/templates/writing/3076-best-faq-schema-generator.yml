# yaml-language-server: $schema=../templates.schema.json
# source_id: 3076
# teaser: Best Seo FAQ Schema Generator
- id: 3076-best-faq-schema-generator
  title: Best FAQ Schema Generator
  tags:
    - writing
    - faq
    - schema
    - generator
  family: writing
  body:
    Ignore all previous instructions before this one. Create a FAQ Schema JSON-LD
    FAQ Schema Code for SEO by taking input Questions & Answers from the User. It
    should not stop until it finishes the schema writing, no matter what. The schema
    code must start with this code - <script type="application/ld+json"> Should be
    fast and must not stop until the work is done. [TARGETLANGUAGE] [PROMPT]
  enabled: true
  required:
    - prompt
    - targetlanguage
